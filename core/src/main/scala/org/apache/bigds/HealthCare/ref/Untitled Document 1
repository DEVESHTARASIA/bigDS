KS, Wilcox 版本类型

KS
1. 打散, 按列aggregate/(collect,parallelize )
2. 列间并行排序
3. a. collect到driver上扫描计算
   b.矩阵broadcast到每个节点扫描计算(减小矩阵体积? partition处理?)

不打散, 直接列RDD处理,双循环

Wilcox
1. 打散, 按列aggregate
2. collect到driver上, 然后broadcast
2. 并行排序

不打散,直接列RDD处理,双循环




